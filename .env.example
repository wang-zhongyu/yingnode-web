# 数据库路径（SQLite 文件）
DATABASE_URL="file:/data/yingnode.db"

# Wi-Fi 网口名称
WIFI_INTERFACE="wlan0"

# 离线热点配置
HOTSPOT_SSID="yingnode"
HOTSPOT_IP="172.16.42.1"

# 认证密钥（必填，用 openssl rand -base64 32 生成）
BETTER_AUTH_SECRET=""

# 终端访问令牌（用 openssl rand -base64 16 生成）
TERMINAL_TOKEN=""
